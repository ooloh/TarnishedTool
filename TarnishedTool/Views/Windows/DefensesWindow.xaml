<Window x:Class="TarnishedTool.Views.Windows.DefensesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Width="180"
        SizeToContent="WidthAndHeight"
        AllowsTransparency="True"
        WindowStyle="None"
        ShowInTaskbar="False"
        Background="#80000000">
    
    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform x:Name="ContentScale" ScaleX="1" ScaleY="1" />
        </Grid.LayoutTransform>
        <Border x:Name="MainBorder" BorderBrush="#80333333" BorderThickness="1" CornerRadius="3"
                Margin="2" Width="170" Background="#80000000">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,0">
                    <DockPanel>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                            <Button Content="−" Width="20" Height="20" FontSize="14"
                                    Background="Transparent"
                                    BorderThickness="0" Foreground="#CCFFFFFF"
                                    Click="DecreaseSize_Click"
                                    Cursor="Hand" />
                            <Button Content="+" Width="20" Height="20" FontSize="14"
                                    Background="Transparent"
                                    BorderThickness="0" Foreground="#CCFFFFFF"
                                    Margin="2,0,0,0"
                                    Click="IncreaseSize_Click"
                                    Cursor="Hand" />
                            <Button Content="◐" Width="24" Height="20" FontSize="14"
                                    Background="Transparent" BorderThickness="0" Foreground="#CCFFFFFF"
                                    Click="CycleOpacity_Click" Cursor="Hand" ToolTip="Opacity" />

                        </StackPanel>

                        <Button Content="✕" Width="16" Height="16" FontSize="8"
                                HorizontalAlignment="Right"
                                Background="Transparent"
                                BorderThickness="0" Foreground="#CCFFFFFF"
                                Click="CloseButton_Click"
                                Cursor="Hand" />
                    </DockPanel>
                </Grid>


                <Grid Grid.Row="1" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="65" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="Margin" Value="0,5,0,0" />
                        </Style>
                    </Grid.Resources>


                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Standard:" FontSize="14" Margin="0,0,0,5"
                               Foreground="#CCFFFFFF" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding StandardDefense, StringFormat={}{0:F1}%}"
                               FontSize="14" Margin="0,0,0,5" Foreground="#CCFFFFFF" HorizontalAlignment="Right" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Slash:" FontSize="14" Margin="0,0,0,5"
                               Foreground="#CCFFFFFF" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SlashDefense, StringFormat={}{0:F1}%}"
                               FontSize="14" Margin="0,0,0,5" Foreground="#CCFFFFFF" HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Strike:" FontSize="14" Margin="0,0,0,5"
                               Foreground="#CCFFFFFF" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding StrikeDefense, StringFormat={}{0:F1}%}"
                               FontSize="14" Margin="0,0,0,5" Foreground="#CCFFFFFF" HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Thrust:" FontSize="14" Margin="0,0,0,5"
                               Foreground="#CCFFFFFF" />
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ThrustDefense, StringFormat={}{0:F1}%}"
                               FontSize="14" Margin="0,0,0,5" Foreground="#CCFFFFFF" HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Magic:" FontSize="14" Margin="0,0,0,5"
                               Foreground="#CCFFFFFF" />
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding MagicDefense, StringFormat={}{0:F1}%}"
                               FontSize="14" Margin="0,0,0,5" Foreground="#CCFFFFFF" HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Fire:" FontSize="14" Margin="0,0,0,5"
                               Foreground="#CCFFFFFF" />
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding FireDefense, StringFormat={}{0:F1}%}"
                               FontSize="14" Margin="0,0,0,5" Foreground="#CCFFFFFF" HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Lightning:" FontSize="14" Margin="0,0,0,5"
                               Foreground="#CCFFFFFF" />
                    <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding LightningDefense, StringFormat={}{0:F1}%}"
                               FontSize="14" Margin="0,0,0,5" Foreground="#CCFFFFFF" HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Holy:" FontSize="14" Margin="0,0,0,5"
                               Foreground="#CCFFFFFF" />
                    <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding HolyDefense, StringFormat={}{0:F1}%}"
                               FontSize="14" Margin="0,0,0,5" Foreground="#CCFFFFFF" HorizontalAlignment="Right"/>

                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>